import 'package:flutter/material.dart';

class InstituteScreen2 extends StatelessWidget {
  const InstituteScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child:   Text("InstituteScreen2"),),
    );
  }
}